{
  "$schema": "https://unpkg.com/release-it/schema/release-it.json",
  "git": {
    "requireBranch": "main",
    "push": true,
    "tagName": "${version}"
  },
  "hooks": {
    "before:init": [
      "git pull",
      "npm run lint"
    ],
    "after:bump": [
      "npm run build",
      "npx auto-changelog --package"
    ]
  },
  "github": {
    "release": true,
    "releaseName": "V${version}",
    "releaseNotes": "sed -n '/^## \\[${version}\\]/,/^## /p' CHANGELOG.md | sed '1d;$d'"
  },
  "npm": {
    "publish": true
  }
}